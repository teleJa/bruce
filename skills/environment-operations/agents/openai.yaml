interface:
  display_name: "Environment Operations"
  short_description: "Use confirmed environments through bounded operation manifests"
  default_prompt: "Use $environment-operations to generate or apply a bounded operation manifest from a confirmed Environment Profile."
