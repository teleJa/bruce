interface:
  display_name: "Environment Profile"
  short_description: "Generate reusable environment profiles safely"
  default_prompt: "Use $environment-profile to record a user-provided and unconfirmed Environment Profile without scanning repository implementation files or executing the project environment."
