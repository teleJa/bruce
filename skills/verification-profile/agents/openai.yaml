interface:
  display_name: "Verification Profile"
  short_description: "Generate requirement-scoped verification and repair profiles"
  default_prompt: "Use $verification-profile with an explicit requirements.md and confirmed Environment Profiles to generate a requirement-scoped verification and repair profile without executing adapters."
