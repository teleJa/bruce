interface:
  display_name: "Environment Profile"
  short_description: "Generate Agent test environments with confirmed scope"
  default_prompt: "Use $environment-profile to generate a compact Agent test and verification Environment Profile with one confirmed scope for build, test data, authentication, and evidence; do not execute operations."
