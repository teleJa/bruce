# 任务 <task-id>：<title>

- 契约修订：1
- 契约状态：执行前冻结
- 状态来源：`../checkpoint.yaml`
- 父计划：`../plan.md`

## 目标

<可观察的任务局部结果>

## 包含范围

- <允许修改的仓库文件/符号或有明确边界的组件；新增文件标注为新增>

## 排除范围

- <本任务不得修改的路径、组件、契约或操作>

## 依赖关系

- 依赖任务：<任务 ID 或无>
- 使用：<上游任务产物、接口签名/数据形状及权威定义位置；无则写无>
- 产出：<供哪个调用方使用的接口/工件及约定；无则写无>
- 并行安全：<parallel_safe: true|false 及共享文件归属/冲突依据>

## 实施安排

- 上游设计依据：<权威文档位置或已确认任务决策及本任务必须保持的约束>
- 源码依据：<已核实的文件/符号、当前职责与相关事实，以及 commit 或内容快照依据；未知项明确标注>
- 必读材料：<执行本任务所需的最小文档章节/接口定义/代码入口及用途，不依赖方案讨论历史>
- 前置条件与实施顺序：<依赖任务的具体产物、共享文件归属和必要执行条件>
- 第一处改动或验证入口：<具体文件/符号及动作；执行前核对当前指令、工作区和目标源码>

### 逐项改动

> 每个改动点填写一项，简单任务只需一项。按可验收的局部结果组织任务，不按行数拆任务。

1. **<动作与对象>**
   - 位置：<真实文件/函数/组件；新增位置明确标注>
   - 当前行为：<已核实的行为或缺失能力，不把推测写成事实>
   - 目标行为：<明确输入、处理条件、输出和副作用；适用时包含失败/空值/重复调用等路径>
   - 实施要求：<必须复用的接口、接通的调用点及必要操作顺序；用权威定义或输入输出示例消除歧义>
   - 保持不变：<该位置仍须保留的行为、接口、数据或相邻功能；不只列禁止修改的文件>
   - 验收关联：<下方验收场景 ID>

### 执行者决策边界

- 开发者可自行决定：<不改变契约、任务边界或验收的局部实现选择>
- 已确定、不得自行重选：<本任务相关的业务规则、接口/数据契约及架构选择，引用权威依据>
- 待解决问题：<无；若存在影响执行的缺失事实/决策，先在拆分结果中报告受阻任务及问题，补齐后再生成本契约>

仅在适用时增加数据迁移、新旧兼容、切换及恢复顺序；不复述整份架构，也不规定逐行编码。
若出现未决业务规则或与已确认架构的冲突，返回具体问题，不把它作为普通开发细节留给执行者。

## 业务不变量与权威状态（按适用性）

> 任务涉及绑定、分配、占用、唯一归属、共享、权限投影、健康度或可用性推导时填写摘要；详细矩阵由
> `test-plan.md` 持有，避免产生两份独立事实源。

- 一致性检查：<required|not_applicable>
- 业务不变量与权威状态摘要：<适用时填写；不适用写原因>
- 竞争者/权限视角与冲突后果：<适用时填写；不适用写原因>
- 关联测试计划矩阵/场景 ID：<CONS-xxx、CONS-S-xxx 或 none>
- 不适用原因：<仅 not_applicable 时填写>

## 验收标准

> 按场景重复填写，覆盖新增结果及必须保持的旧行为；适用时包含拒绝/失败路径。
> 复用父级验收与 test-plan 场景 ID；需要局部细分时明确其父级映射。

- 父级场景 ID：<稳定验收 ID>
- Given：<起始状态>
- When：<任务动作或变更>
- Then：<可观察的任务局部结果>
- Evidence：<所需证据 ID、路径或命令>

## 验证

- 必需层级：<unit|integration|API|database|build|real-use|browser>
- 命令/检查：<具体检查>
- 环境：<所需运行时、数据库、浏览器环境或无>
- 预期可观察结果：<本任务对应的验证断言；计划中的检查不代表已执行或通过>

## 授权与风险

- 授权：<normal|explicit capability|user confirmation>
- 风险触发：<low|guarded|critical 及原因>
- 停止条件：<源码与方案关键事实不符、依赖未就绪、需要越界或变更已确认决策等具体触发条件>
- 返回内容：<受阻改动点、实际事实及证据、影响与所需决策；不得静默扩范围或补设计>

## 契约变更规则

不得静默扩大本任务范围。如果范围、验收、依赖、授权或所需验证层级发生变化，必须创建新的契约修订或替代任务，并在下一个检查点中记录原因。
